#include <iostream>
#include <vector>
const int INF = 1e9;
using namespace std;

int main() {
  int N;
  cin >> N;
  vector<int> P(N);
  for (int i = 0; i < N; ++i) cin >> P[i];

  vector<int> Q(200000);
  for (int i = 0; i < 200000; ++i) Q[i] = i;
  for (int n = 0; n < N; ++n) {

  }
  cout << ans << endl;
}